name: Test
on: push

jobs:
  test1:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: echo "123" > ./dir/file1.txt

      - uses: EndBug/add-and-commit@dist
        with:
          message: "Test1"
          path: "."
          pattern: "*.txt"
          force: false
          find_args: '-not -path "./dir/*"'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
