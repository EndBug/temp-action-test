name: Add tag
on:
  release:
    types: [published]

jobs: 
  run:
    name: Tag
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@master

    - uses: 'EndBug/latest-tag@docker'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}