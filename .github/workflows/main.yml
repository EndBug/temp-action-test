name: Check version
on: push

jobs: 
  run:
    name: Check version
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@master

    - name: Check
      id: check
      uses: EndBug/version-check@master

    - run: 'echo ${{ steps.check.changed }} ${{ steps.check.type }}'