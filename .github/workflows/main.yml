name: Test
on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: echo "123" > file.txt

      - uses: EndBug/add-and-commit@cwd
        with:
          message: "Test"
          path: "."
          pattern: "*.txt"
          force: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
