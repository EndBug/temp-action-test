name: Add tag
on: push

jobs: 
  run:
    name: Tag
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@master

    - name: Set up Node.js
      uses: actions/setup-node@master
      with:
        node-version: 10.0.0

    - uses: ./action
      with:
        r1: [1, 2, '3']
