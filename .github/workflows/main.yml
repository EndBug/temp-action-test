name: Test
on:
  release:
    types: [published]

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: EndBug/latest-tag@master
        with:
          description: annotated to annotated

        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
